{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Simulation (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "terminal": "integrated"
        }
    ]
}